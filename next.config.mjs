/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "lh3.googleusercontent.com",
      "www.tylebahehesyj.ws",
    ],
  },
};

export default nextConfig;
